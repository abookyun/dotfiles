# Default pip packages to install automatically with each Python version
# https://github.com/asdf-community/asdf-python#default-python-packages

# Keep this minimal - install project-specific packages in virtual environments
pip
setuptools
wheel

# Editor tooling. These live here rather than in brew because asdf installs
# packages per Python version, so anything missing from this file silently
# disappears on the next `asdf install python`.
basedpyright
ruff
