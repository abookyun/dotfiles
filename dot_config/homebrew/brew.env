# Homebrew environment configuration
# See: https://docs.brew.sh/Manpage#environment
#
# This file is automatically loaded by Homebrew from:
# $XDG_CONFIG_HOME/homebrew/brew.env (user-specific)
#
# NOTE: This is NOT a shell script. Do not use 'export' keyword.
# Quotation marks are taken literally.

# Disable analytics (privacy protection)
HOMEBREW_NO_ANALYTICS=1

# Disable auto-update before running brew install, brew upgrade, etc.
# You can manually update with: brew update
HOMEBREW_NO_AUTO_UPDATE=1

# Do not create Brewfile.lock.json when running brew bundle
# For personal use, strict version locking is usually not needed
HOMEBREW_BUNDLE_NO_LOCK=1

# Disable Cargo package dumping in brew bundle dump
# Cargo is managed by asdf, and shim execution causes issues in brew's subprocess
HOMEBREW_BUNDLE_DUMP_NO_CARGO=1
