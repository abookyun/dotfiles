# uv configuration
# https://docs.astral.sh/uv/concepts/configuration-files/

# Note: Tool directories are configured via environment variables in shell config:
# UV_TOOL_DIR, UV_TOOL_BIN_DIR, UV_CACHE_DIR
